Методи підключенн скрипта (тег скрипт і окремий файл)

Виконання скрипта, завантаження і рендер сторінки

Можна підключити кілька скриптів

атрибути async  i defer

async - браузер не припиняє роботу і продовжує завантажувати розмітку, а коли скрипт повністю завантажиться відразу виконає його

defer - гарантує порядок виконання скриптів з цим атрибутом браузер теж не припиняє роботу, але чекає повного завантаження сторінки

Ідентифікатор - імя змінної;
Ситнаксис:
1) чутливий до реєстру;
2) перший символ A-Z , a-z _ , $;
3) camelCase;
4) Без трансліта;

Коментарі
// -  ctrl /
/**/ - ctrl shift /

"use strict"

Змінні - місце в памятті для збереження даних;
var, let, const.
принцип створення змінної:
1) ключове слово var, let, const.
2) знак = присвоєння
3) запис значення
кілька змінних через кому

Типип даних в js:
1)Number
2)String - правила написання
3) boolean
4) null - нічого, значення невідоме
5) undefined - значення не присвоєно
6) object - обєкт(ключ і значення)
7) Symbol - символ
метод typeof

Інтерактивність
alert, console.log
prompt,confirm

Математичні оператори
+ NaN
-
/
*
%

Перетворення на число
+
parseInt
parseFloat
isNaN
toFixed();
дробові числа


Строки

Шаблоні стороки
Екранування

Доступ до символа через [];

Методи сторок

length - str.length;
toLowerCase() - str.toLowerCase()
toUpperCase() - str.toUpperCase()
split() - str.split(' ')
join() - arr.join(' ')
substring() - str.substring(start,end not include);
substr() - str.substr (start, end include);
charAt() - Символ стороки
charCodeAt() - букау в юнікод
concat() - str1.concat(str2, str3)
endsWith()
startsWith() - str.startsWith("Hello");
fromCharCode() -
includes() - str.includes('')
indexOf()
lastIndexOf()
repeat() - str.repeat(2)
slice()
reverse()
trim()